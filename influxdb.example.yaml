influxdb:
  username: hass
  password: !secret influxdb_password
  max_retries: 3
  default_measurement: state
  database: hass
  include:
    entity_globs:
      - sensor.radsens_*
      - sensor.geiger_counter_*
